---
description: Generate a design token set (typography, spacing, radius, colors) for a premium UI.
---

Create a token system for: "$ARGUMENTS"

Output both:
1) CSS variables
2) Tailwind-friendly mapping guidance

Keep tokens minimal, consistent, and mobile-first.
